# 🚀 Швидкий Деплой на Railway (БЕЗКОШТОВНО)

## Найпростіший спосіб виставити магазин в інтернет

### Крок 1: Підготовка

1. Створи акаунт на GitHub: https://github.com/signup
2. Встанови GitHub Desktop: https://desktop.github.com/
3. Створи акаунт на Railway: https://railway.app/

### Крок 2: Завантаж проект на GitHub

1. Відкрий GitHub Desktop
2. File → Add Local Repository → обери папку `order-service`
3. Якщо попросить "Initialize", натисни Yes
4. Напиши коміт-повідомлення (наприклад "Initial commit")
5. Натисни "Publish repository"
6. Зроби репозиторій Public або Private (як хочеш)

### Крок 3: Важлива зміна в коді

Перед деплоєм відкрий файл `public/app.js` і знайди рядок:
```javascript
const API_URL = 'http://localhost:3001/api';
```

Зміни на:
```javascript
const API_URL = window.location.hostname === 'localhost' 
  ? 'http://localhost:3001/api' 
  : '/api';
```

Це потрібно щоб додаток працював і локально, і на сервері.

Збережи зміни і зроби новий коміт в GitHub Desktop:
- Summary: "Fix API URL for production"
- Push to origin

### Крок 4: Деплой на Railway

1. Зайди на https://railway.app/
2. Натисни "Login" → авторизуйся через GitHub
3. Натисни "New Project"
4. Обери "Deploy from GitHub repo"
5. Обери репозиторій `order-service`
6. Railway автоматично:
   - Визначить що це Node.js проект
   - Встановить залежності (`npm install`)
   - Запустить сервер (`npm start`)

### Крок 5: Отримай публічний URL

1. В Railway dashboard клікни на свій проект
2. Перейди на вкладку "Settings"
3. Знайди "Domains" → натисни "Generate Domain"
4. Скопіюй URL (буде щось типу `your-app.up.railway.app`)

### ГОТОВО! 🎉

Твій магазин тепер доступний в інтернеті!

Відкрий URL в браузері і все має працювати.

---

## Важливо знати

### Безкоштовний план Railway:
- $5 кредитів на місяць (безкоштовно)
- Цього вистачить на ~300-400 годин роботи
- Якщо не використовуєш - сервер засипає
- При запиті - автоматично просинається (може 2-3 сек)

### Оновлення сайту:
1. Зміни код на комп'ютері
2. Зроби коміт в GitHub Desktop
3. Натисни "Push"
4. Railway автоматично оновить сайт за ~1-2 хвилини

### Логіни за замовчуванням:
- Логін: `admin`
- Пароль: `admin123`

**ОБОВ'ЯЗКОВО зміни пароль після першого входу!**

---

## Альтернатива: Render.com

Якщо Railway не підходить, спробуй Render.com (також безкоштовний):

1. Зайди на https://render.com/
2. Signup → Connect GitHub
3. New → Web Service
4. Обери репозиторій
5. Налаштування:
   - Name: `my-shop`
   - Build Command: `npm install`
   - Start Command: `npm start`
6. Create Web Service

Render дає постійний URL без обмежень на сон сервера.

---

## Проблеми?

### База даних скидається після рестарту
Railway перезапускає сервер кожні 24 години на безкоштовному плані, і база може скинутися.

**Рішення:** Використай PostgreSQL (інструкція окремо) або перейди на платний план.

### Картинки не зберігаються
Файлова система Railway тимчасова. Картинки можуть зникати.

**Рішення:** Використай Cloudinary для зберігання картинок (інструкція окремо).

---

## Наступні кроки

Коли все запрацює, можна:
- Підключити власний домен (наприклад `myshop.com`)
- Додати базу даних PostgreSQL (для надійності)
- Налаштувати Cloudinary для картинок
- Додати Google Analytics

Просто скажи що потрібно, і я допоможу! 🚀
